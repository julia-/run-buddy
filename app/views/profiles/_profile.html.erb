<div>
  <h3><%= profile.first_name %> <%= profile.last_name %></h3>
  <figure>
    <% if profile.profile_photo_data? %>
      <%= link_to image_tag(profile.profile_photo_url(:thumb)), profile %>
    <% end %>
  </figure>
</div>
